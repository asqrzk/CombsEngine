referencesExtracted

documentations

AGENTS.md

.DS_Store

# Rust build artifacts (multi-GB)
Engine/Core/target/
**/target/

# Cross-platform bundled artifacts (rebuilt via `cargo xtask bundle`)
Engine/Core/dist/

# Downloaded model weights (re-pull via presets / combs pull)
models/
*.safetensors
*.gguf
*.bpk

# Deno / JS
Engine/Js/**/node_modules/
**/.deno/

# Logs & temp
*.log
.env
kimi-export-*.md

# UI build artifacts
Engine/Ui/**/node_modules/
Engine/Ui/**/dist/
Engine/Ui/**/.svelte-kit/

# Runtime state generated by the proxy during dev (per-scaffold)
**/server/data/
**/server/master.key
**/server/manifest.json
**/server/permissions.json
**/server/authn.json
**/server/*.log
**/combs-serve.log
**/observe.ndjson

# Local npm tarballs (rebuilt via `npm pack`; publish from CI)
Engine/Packages/npm/**/*.tgz

# Playwright / test artifacts
/test-results/
/playwright-report/
/tmp-*/

# Python build artifacts
Engine/Packages/pypi/**/dist/
Engine/Packages/pypi/**/*.egg-info/
__pycache__/
target-f16/
target-lora/

# Internal reasoning / measurement docs — local-only, never publish.
/*_PLAN.md
/docs/
/documentations/

# Browser engine build output (rebuilt via `cargo xtask web`): a wasm
# module tens of MB large, plus its generated wasm-bindgen shim.
engine/js/core/pkg/
